#ifndef LAYOUT_H
#define LAYOUT_H

enum {
    WIN_WIDTH  = 1280,
    WIN_HEIGHT = 512,
    INTERVAL = 16,  // update interval (60 fps)
    SPEED = 5,
    STARTING_POSITION = (WIN_HEIGHT/2) - 20
};

#endif